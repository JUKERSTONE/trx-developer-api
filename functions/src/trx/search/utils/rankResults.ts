import { TRX_00 } from "../../../types";

export const rankResultsQuery = async ({
  localResults,
  webResults,
}: {
  localResults: TRX_00[];
  webResults: TRX_00[];
}): Promise<TRX_00[]> => {
  //
  //

  return [];
};
